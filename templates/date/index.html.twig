{% extends 'base.html.twig' %}

{% block title %}7days - Task #1{% endblock %}

{% block body %}
    {% if dateHandler %}
    <article>
        The time zone {{ dateHandler.timezone }} has {{ dateHandler.minutesOffsetFromUtc }} minutes offset to UTC on the given day at noon.<br />
February in this year is {{ dateHandler.februaryDays }} days long.<br />
The specified month ({{ dateHandler.currentMonthName }}) is {{ dateHandler.currentMonthDays}} days long.
    </article>
    {% endif %}

    {{ form(form) }}

{% endblock %}
